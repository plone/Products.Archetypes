<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      i18n:domain="plone">

  <body>
    <div metal:define-macro="referenceBox" 
      tal:define="
      ct here/archetype_tool;
      refs python: ct.getRefs(here); 
      brefs python: ct.getBRefs(here);
      "
      >
      <!-- Reference Box-->
      <div class="box" tal:condition="python: refs or brefs">
	<h5>References</h5>
	<div class="body">
	  <div tal:condition="refs">
	  <strong>Cites</strong> 
	  <br />
	    <div class="odd" tal:repeat="ref refs">
	      <a href="#" tal:attributes="href python:ct.getObject(ref).absolute_url()"><img tal:attributes="src python: ct.getObject(ref).getIcon()"/> <span tal:content="python: ct.getObject(ref).Title()"/></a>
	</div>
	</div>
	<div tal:condition="brefs">
	<strong>Cited By</strong> 
	<div class="even" tal:repeat="ref brefs">
	  <a hrf="#" tal:attributes="href python:ct.getObject(ref).absolute_url()"><img tal:attributes="src python: ct.getObject(ref).getIcon()"/> <span tal:content="python: ct.getObject(ref).Title()"/></a>
	</div>
      </div>
      </div>
    </div>
  </div>
  </body>
  
</html>

